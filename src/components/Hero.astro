---
const { title, subtitle, cta } = Astro.props;
---
<section class="bg-[linear-gradient(to_bottom_right,_#CADACB_10%,_#F8FFF8_90%)] relative">
  <div class="container mx-auto py-16 flex flex-col items-center text-center gap-6 lg:py-24">
    
    <!-- Texte et boutons -->
    <div class="max-w-3xl">
      <h1 class="text-3xl sm:text-4xl lg:text-5xl font-semibold text-gray-900 leading-snug">{title}</h1>
      <p class="mt-4 text-lg text-gray-700">{subtitle}</p>

      <div class="mt-6 flex flex-col sm:flex-row sm:justify-center gap-4">
        <a
          href="#contact"
          class="inline-block rounded-md bg-primary px-6 py-3 text-white shadow hover:opacity-95 focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2"
        >
          {cta}
        </a>
        <a
          href="#vie"
          class="inline-block rounded-md border border-primary px-6 py-3 text-primary hover:bg-primary hover:text-white focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2"
        >
          Découvrir
        </a>
      </div>
    </div>

    <!-- Image hero -->
    <div class="mt-10 w-full max-w-5xl rounded-lg overflow-hidden shadow-lg">
      <img
        src="/images/hero.webp"
        alt="Vue extérieure Résidence Les Lauriers"
        loading="lazy"
        srcset="/images/hero.webp 1x"
        sizes="(max-width: 768px) 100vw, 100vw"
        class="w-full h-auto object-cover"
      />
    </div>

    <!-- Flèche incitation au scroll -->
    <a href="#vie" class="absolute bottom-6 animate-bounce" aria-label="Scroll vers le bas">
      <svg class="w-8 h-8 text-primary" fill="currentColor" viewBox="0 0 24 24">
        <path d="M12 16l-6-6h12l-6 6z"/>
      </svg>
    </a>
  </div>
</section>
